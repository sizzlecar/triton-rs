//! `scf` dialect — structured control flow.
//!
//! Phase 2 placeholder. Will populate `scf.if`, `scf.for`, `scf.while`,
//! `scf.yield` once the first kernel that needs control flow lands
//! (matmul / attention).
